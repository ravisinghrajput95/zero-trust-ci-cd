name: Zero Trust CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  build:
    name: Build & Scan Artifacts
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN }}

      - name: Build Docker Image
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/my-image:${{ github.sha }} .

      - name: Scan Image with Trivy
        uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: '${{ secrets.DOCKERHUB_USERNAME }}/my-image:${{ github.sha }}'
          format: 'table'
          exit-code: '0'                   #set to 1 in case to fail the build
          severity: 'CRITICAL'

      - name: Push Image to Docker Hub
        run: |
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/my-image:${{ github.sha }}

  sign:
    name: Sign and Verify Artifacts
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Install Cosign
        run: |
          curl -LO https://github.com/sigstore/cosign/releases/latest/download/cosign-linux-amd64
          chmod +x cosign-linux-amd64
          sudo mv cosign-linux-amd64 /usr/local/bin/cosign

      - name: Decode Cosign Key
        run: echo "${{ secrets.COSIGN_KEY }}" | base64 --decode > cosign.key

      - name: Sign Image with Cosign
        env:
          COSIGN_PASSWORD: ${{ secrets.COSIGN_PASSWORD }}
          COSIGN_YES: "true"   # <--- This auto-confirms signing
        run: |
          cosign sign --key cosign.key ${{ secrets.DOCKERHUB_USERNAME }}/my-image:${{ github.sha }}

      - name: Verify Signature
        run: |
          cosign verify ${{ secrets.DOCKERHUB_USERNAME }}/my-image:latest \
            --certificate-identity "https://github.com/${{ github.repository }}/actions" \
            --certificate-oidc-issuer "https://token.actions.githubusercontent.com"

